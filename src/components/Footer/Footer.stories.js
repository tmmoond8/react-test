import React from "react";
import {} from "@storybook/addon-actions";
import Footer from ".";

export default {
  title: "Footer",
  component: Footer,
};

export const Default = () => {
  return <Footer />;
};
